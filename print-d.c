#include "main.h"

/**
 * print_integer - print integer
 * @args:..
 *
 * Return:..
 */
/*
int print_integer(va_list args)
{
	int nb = va_arg(args, int);
	int digit, expon = 1;
	if (nb < 0)
	{
		_putchar('-');
		nb = nb * -1;
	}
	while (nb != 0)
	{
		digit = nb % 10;
		expon = expon * 10 + digit;
		nb = nb / 10;
	}	
	while (expon > 0)
	{
		digit = expon % 10;
		_putchar(digit + '0');
		expon = expon / 10;
	}

	return (nb);
}*/
